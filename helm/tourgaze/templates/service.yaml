apiVersion: v1
kind: Service
metadata:
  name: {{ include "tourgaze.fullname" . }}
  labels:
    {{- include "tourgaze.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tourgaze.selectorLabels" . | nindent 4 }}
