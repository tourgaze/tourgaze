# Default values for tourgaze.
replicaCount: 1

image:
  repository: mschwehl/tourgaze
  pullPolicy: IfNotPresent
  tag: "latest"

service:
  type: ClusterIP
  port: 8085

persistence:
  enabled: true
  size: 5Gi
  accessMode: ReadWriteOnce
  # storageClass: ""

# ⚠️ TourGaze has NO built-in authentication (single-user, local-first app).
# An enabled Ingress with no auth exposes your whole library to anyone who can
# reach the host. For anything beyond a trusted private network, terminate TLS
# and add authentication at the ingress (e.g. nginx auth-url / basic-auth,
# Traefik forward-auth, or an auth proxy like oauth2-proxy / Authelia).
ingress:
  enabled: true
  className: ""
  annotations:
    # Ride uploads (FIT/GPX/KMZ + photos) can be large.
    nginx.ingress.kubernetes.io/proxy-body-size: "100m"
    traefik.ingress.kubernetes.io/router.entrypoints: web
  hosts:
    - host: tourgaze.localhost
      paths:
        - path: /
          pathType: Prefix
    - host: localhost
      paths:
        - path: /
          pathType: Prefix
  # tls:
  #   - secretName: tourgaze-tls
  #     hosts: [tourgaze.localhost]

# The image's entrypoint already passes --app.start-browser=false and sets
# -Djava.awt.headless=true; only the data dir needs wiring to the volume.
env:
  TOURGAZE_DATA_DIR: "/data"

resources:
  limits:
    memory: "1Gi"
    cpu: "1000m"
  requests:
    memory: "512Mi"
    cpu: "250m"

# Generous delays: Spring Boot + H2/Flyway boot takes ~10s.
livenessProbe:
  httpGet:
    path: /actuator/health
    port: http
  initialDelaySeconds: 30
  periodSeconds: 20
  failureThreshold: 3

readinessProbe:
  httpGet:
    path: /actuator/health
    port: http
  initialDelaySeconds: 20
  periodSeconds: 10
  failureThreshold: 3
